# Convert a list of strings ["apple", "banana", "cherry"] to uppercase using list comprehension.

in_string = ["apple", "banana", "cherry"]

upper_case = [i.upper() for i in in_string]
print(upper_case)